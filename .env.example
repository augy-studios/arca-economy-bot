# Bot credentials
BOT_TOKEN=your_bot_token_here

# Guild ID — sync commands to your server only (faster)
GUILD_ID=your_guild_id_here

# All other settings (economy limits, anti-exploit, timeouts, etc.)
# are configured per-guild at runtime with /admin config set
